public class Collections{
    public static void main(String args[]){
        System.out.println("Collections");
    }
}
/*
 * data will store in object form in the collections
 * method
 * add()
 * remove()
 * size()
 * clear()
 * contains(Object element)
 * iterator
 * isEmpty()
 * equals()->It matches two collections.
 * hashCode()
 * Stream<E> stream()->it returns a sequential Stream with the collection as its source.
 * 
 * iterator inface methodes
 * hasNext()->It returns true if the iterator has more elements otherwise it returns false.
 * next() ->it returns the element and moves the cursor pointer to the next element.
 * remove()
 */